'use strict';

import React from 'react';
import ReactDOM from 'react-dom';

import 'normalize.css';

import Squash from './components/Squash';

ReactDOM.render(
  React.createElement(Squash),
  document.getElementById('root')
);
